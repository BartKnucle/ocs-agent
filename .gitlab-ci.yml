stages:
  - install
  - build
  - quality

install:
  stage: install
  tags:
    - test
  script:
    - npm install
  artifacts:
    name: "artifacts"
    expire_in: 1 week
    untracked: true
    paths:
      - node_modules/

build:
  stage: build
  tags:
    - test
  script:
    - CI=false npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - dist
      - .nuxt
  dependencies:
    - install

linting:
  stage: quality
  tags:
    - test
  script:
    - npm run lint
  dependencies:
    - build

run_tests:
  stage: quality
  tags:
    - test
  script:
    -  npm run test
  dependencies:
    - build
  coverage: /All files[^|]*\|[^|]*\s+([\d\.]+)/